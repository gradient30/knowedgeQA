// P1功能验证 - 文件上传入口测试\nconst { chromium } = require('playwright-core');\n\nasync function testP1FileUpload() {\n  console.log('🚀 开始P1功能验证 - 文件上传入口');\n  \n  const browser = await chromium.launch({ headless: false });\n  const page = await browser.newPage();\n  \n  try {\n    // 1. 访问登录页面\n    console.log('📝 步骤1: 访问登录页面');\n    await page.goto('http://localhost:3000/login');\n    await page.waitForTimeout(2000);\n    \n    // 2. 登录\n    console.log('🔐 步骤2: 用户登录');\n    await page.fill('input[placeholder*=\"邮箱\"]', 'testuser@example.com');\n    await page.fill('input[type=\"password\"]', 'Password123!');\n    await page.click('button[type=\"submit\"]');\n    await page.waitForTimeout(3000);\n    \n    // 3. 检查导航栏是否包含文件中心链接\n    console.log('🔍 步骤3: 检查导航栏文件中心入口');\n    const filesCenterLink = await page.locator('a[href=\"/files\"]').count();\n    console.log(`文件中心链接数量: ${filesCenterLink}`);\n    \n    if (filesCenterLink === 0) {\n      throw new Error('❌ 导航栏中未找到文件中心入口');\n    }\n    \n    // 4. 点击文件中心链接\n    console.log('📁 步骤4: 访问文件中心页面');\n    await page.click('a[href=\"/files\"]');\n    await page.waitForTimeout(2000);\n    \n    // 5. 检查文件中心页面是否正确加载\n    const currentUrl = page.url();\n    console.log(`当前页面URL: ${currentUrl}`);\n    \n    if (!currentUrl.includes('/files')) {\n      throw new Error('❌ 未正确跳转到文件中心页面');\n    }\n    \n    // 6. 检查文件上传组件是否存在\n    console.log('📤 步骤5: 检查文件上传组件');\n    const uploadArea = await page.locator('.ant-upload-drag').count();\n    console.log(`上传区域数量: ${uploadArea}`);\n    \n    if (uploadArea === 0) {\n      throw new Error('❌ 未找到文件上传组件');\n    }\n    \n    // 7. 检查文件管理标签页\n    console.log('📋 步骤6: 检查文件管理功能');\n    const manageTab = await page.locator('div[role=\"tab\"]:has-text(\"文件管理\")').count();\n    console.log(`文件管理标签页数量: ${manageTab}`);\n    \n    if (manageTab === 0) {\n      throw new Error('❌ 未找到文件管理标签页');\n    }\n    \n    // 8. 截图保存验证结果\n    await page.screenshot({ path: 'p1-file-upload-verification.png', fullPage: true });\n    \n    console.log('✅ P1功能验证成功！');\n    console.log('📊 验证结果:');\n    console.log('  ✅ 文件中心导航入口存在');\n    console.log('  ✅ 文件中心页面正常加载');\n    console.log('  ✅ 文件上传组件正常显示');\n    console.log('  ✅ 文件管理功能可访问');\n    \n  } catch (error) {\n    console.error('❌ P1功能验证失败:', error.message);\n    await page.screenshot({ path: 'p1-file-upload-error.png', fullPage: true });\n  } finally {\n    await browser.close();\n  }\n}\n\n// 运行测试\ntestP1FileUpload().catch(console.error);"